version: '3.8'
services:
  web:
    build: .
    container_name: my-app
    ports:
      - "5000:5000"

  test:
    build: .
    container_name: my-app-tests
    command: ["pytest", "--maxfail=1", "--disable-warnings", "-v"]